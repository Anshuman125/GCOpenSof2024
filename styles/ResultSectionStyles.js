import { StyleSheet } from 'react-native';

export const resultsSectionStyles = StyleSheet.create({
  resultsHeading: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 24,
    marginLeft: 4,
    marginRight: 15,
  },
  resultsContentContainer: {
    flexDirection: 'row',
    paddingHorizontal: 15,
  },
  cardsContainer: {
    flexDirection: 'row',
    justifyContent: 'flex-start',
  },
  // Add more styles as needed
});
